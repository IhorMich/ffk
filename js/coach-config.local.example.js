// Reference values from Supabase Project Settings → API.
// Copy these values into js/coach-config.js for a local test build.
// The anon key is designed for client apps; never put the service_role key here.
window.FFK_COACH_CONFIG = {
  supabaseUrl: 'https://YOUR_PROJECT.supabase.co',
  supabaseAnonKey: 'YOUR_ANON_KEY',
  mode: 'supabase'
};
